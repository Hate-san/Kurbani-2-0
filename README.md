Run `npm install` in the project folder. This pulls in Express, React, Vite, and the other packages listed in package.json.

Run `npm run dev`. This runs `tsx server.ts`, which starts an Express server on port 3000 and uses Vite in middleware mode to serve the React frontend — so one command runs both frontend and backend together.

Visit http://localhost:3000 in your browser. 
